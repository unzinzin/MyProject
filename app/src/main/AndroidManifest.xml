<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myproject">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyProject">
        <activity android:name=".historique.HistoriqueAdapter"></activity>
        <activity android:name=".localisation.LocalPreferences" />
        <activity android:name=".historique.HistoriqueActivity" />
        <activity
            android:name=".settings.AdapterSettings"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".settings.SettingsItem"
            tools:ignore="Instantiatable" />
        <activity android:name=".settings.SettingsRecyclerView" />
        <activity android:name=".localisation.LocalisationActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity" />
    </application>

</manifest>